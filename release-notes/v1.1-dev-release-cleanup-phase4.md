# v1.1-dev release cleanup phase 4

This phase starts the final prose and scaffolding cleanup. It is intentionally conservative. It archives package-era scaffolding and temporary helper scripts, adds guidance for reducing generated-looking phrasing, and keeps only durable course validation checks active.

It does not run MkDocs strict mode, change lab behavior, or rewrite course content automatically.
